# Instructions for Codex

- Work only inside this repository.
- Do not run `git push`.
- Do not add database migrations.
- Before handing over changes, prefer lightweight checks first. Run dependency installs, dev servers and production builds only when explicitly approved.
